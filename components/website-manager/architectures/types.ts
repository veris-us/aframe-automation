import type { DemoContent } from "@/components/builder/types";

export type ArchitectureProps = {
  content: DemoContent;
  isDark: boolean;
  primary: string;
  secondary: string;
  tertiary: string;
  layoutPreset: string;
};